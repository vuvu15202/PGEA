// ** Auth Endpoints
export const jwtConfig = {
  logoutEndpoint: '/login',
  tokenType: 'Bearer',
  storageTokenKeyName: 'accessToken',
  expires: 'expires',
  userData: 'userData',
  refreshToken: 'refreshToken',
};
